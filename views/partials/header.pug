header.header
    .header_column
        a(href=routes.home)
            i.fab.fa-youtube
    .header_column
        form(action=routes.search, method="get")
            input(type="text", placeholder="Search by terms",name="terms")
    .header_column
        ul 
            li  
                a(href=routes.join) Join
            li
                a(href=routes.login) log in